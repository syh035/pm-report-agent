# PM Report Agent · 架构与运行逻辑说明

> 版本 0.10.0 ｜ 面向使用者/维护者的底层运行逻辑阐释
> 目标：让不了解代码的人也能明白——**每个代码文件干什么、每个功能背后的判断逻辑是什么、数据是怎么一步步变成周报的**。

---

## 一、这是什么

把 Excel / CSV / Word / PDF 等**混合格式**的项目资料，自动提炼为结构化任务进度，再用**规则引擎统计 + AI 生成**专业周报的工具。全部可离线运行（AI 为可选增强，未配置 Key 自动回退规则文案）。

启动：`cd pm-report-agent && .venv/bin/python -m uvicorn app:app --port 8000` → http://127.0.0.1:8000

---

## 二、目录结构与各代码文件职责

```
pm-report-agent/
├── app.py                      ★ Web 面板后端（FastAPI）：全部 HTTP 接口、工作区内存态、历史/统计持久化
├── run.py                      CLI 入口（透传给 pmo_report/cli.py）
├── web/index.html              ★ 前端单文件面板（原生 JS，无构建）：数据/规则/模板/周报/设置五个标签页
├── requirements.txt            依赖清单
├── README.md                   面向使用者的快速开始
├── ROADMAP.md                  完善规划（已完成项/待完善项）
├── 架构与运行逻辑说明.md        本文档
├── config/
│   ├── keys.json                ★ AI 密钥/模型/接口地址（仅本地，gitignored，权限 600）
│   ├── rules.json               规则库（阈值+颜色+忽略词+列名映射+状态词映射，运行时生成）
│   └── rules_history.json       规则变更历史（最多 50 条）
├── history/                    历史周报 + 统计快照/缓存（gitignored）
│   ├── stats_history.json       统计快照（环比/趋势的数据源）
│   ├── ai_cache.json            AI 结果缓存（相同输入 0 Token 复用）
│   └── token_usage.json         Token 用量统计
├── examples/                    示例数据（input/）与黄金测试基线（golden/）
├── tests/                       自动化测试（见"测试体系"）
└── pmo_report/                  ★ 核心库（不依赖 FastAPI，可独立用）
    ├── models.py                数据模型：Task（任务）、Project（项目）、常量
    ├── parsers/                 解析层：把各格式文件 → Project
    │   ├── __init__.py          入口 parse_file()：按扩展名分发
    │   ├── tabular_parser.py    表格解析（Excel/CSV）：列名映射/表头识别/进度日期归一化
    │   ├── text_parser.py       文本解析（Word/PDF）：正则切任务/忽略词/模糊行/AI 兜底
    │   └── _date_util.py        日期解析（8 种格式 + Excel 序列号 + 美式日期）
    ├── engine.py                ★ 规则引擎：风险分级/依赖/关键路径/权重/健康体检
    ├── rules.py                 规则库管理：默认值/规范化/读写/历史/自定义规则访问器
    ├── report.py                ★ 周报生成：HTML 模板/JSON blocks 渲染/数字校正/环比
    ├── template_schema.py       模板模块定义/默认 blocks/预置模板
    ├── ai.py                    ★ AI 层：DeepSeek 客户端/缓存/Token 统计/提炼/模板解析
    ├── export.py                HTML → Word/纯文本
    └── cli.py                   命令行入口（report/template 子命令）
```

---

## 三、端到端数据流（一张图）

```
上传文件（Excel/CSV/Word/PDF）
   │
   ▼
【解析层 parsers/】     格式清洗 → 列名映射/表头识别 → 任务提取（规则→模糊行→AI 兜底）
   │                     产出 Project（统一模型：任务/负责人/进度/日期/状态/依赖/权重）
   ▼
【规则引擎 engine.py】  每个任务：状态归一化 → 滞后天数 → 目标进度(曲线) → 风险分级
   │                     项目级：依赖传递 → 关键路径(CPM) → 完成率(权重) → 健康体检
   ▼
【工作区 app.py】        多个 sheet/分组；可人工校对（编辑/删除→回写规则库）
   │
   ▼
【报告生成 report.py】  规则统计表 + AI 综述（缓存+数字校正） + 环比（同集合/无进展）
   │                     按模板（blocks 或 HTML）渲染成周报
   ▼
导出 Word/纯文本/明细 CSV → 存入 history/（统计快照供下周环比与趋势图）
```

---

## 四、已有功能总览（截至 v0.10.0）

**数据接入**：Excel(多 sheet)/CSV(GBK/BOM/参差行)/Word/PDF(扫描 OCR)；表头标题行识别；列名别名映射（内置 100+ 别名 + 用户自定义）；进度写法兼容（50 / 50% / 50％ / 0.5 / 50 %）；日期 8 种格式 + Excel 序列号。

**统计与风险**：完成率（支持任务权重）、平均进度、目标进度（线性/S 曲线）、滞后天数、风险三级（正常/关注/风险）、未按时启动预警、显式状态优先、依赖风险传递、**关键路径 CPM**、数据健康体检（缺进度/负责人/日期/日期倒挂/短名/超100%）。

**任务管理**：校对页增删改任务、偏慢豁免、权重、依赖；删除→忽略词、状态修正→状态词学习（**回写闭环**）；AI 与规则不一致的 diff 标注与一键还原；明细导出 CSV。

**规则库**：阈值与颜色可视化配置；目标进度曲线；**忽略词库 / 自定义列名映射 / 自定义状态词映射**（本地持久化、可导入导出、有历史快照）。

**周报**：HTML 富文本无 markdown；模块化 JSON blocks 模板（拖拽设计、3 套预置模板、实时预览、环比模块）；AI 增强（综述+下周计划，**数字自动校正**防编造）；**环比**（完成率/风险/滞后/同任务集合/连续两周无进展）；历史保存/查看/重命名/删除/按项目分组；项目趋势图。

**AI 成本**：结果磁盘缓存（同输入 0 Token）、规则预筛瘦身、仅模糊行送 AI、Token 用量统计面板、模型/接口可配置、网络重试 1 次。

**体验**：深色模式、打印友好、表格固定表头/分页/图例/行悬停、首次引导、生成耗时、数据体检前置确认。

---

## 五、内部判断逻辑详解

### 5.1 解析层（pmo_report/parsers）

**列名映射（tabular_parser）**：内置 `COLUMN_ALIASES`（标准字段 → 常见叫法列表，中英文）。匹配时把表头转小写去空格后与别名**精确比对**；再叠加用户自定义映射 `{表头: 字段}`。未识别列不报错（字段缺省），由健康体检提示。

**表头识别**：真实台账表头之上常有"XX项目7月进度表"标题行。取前 10 行，统计每行命中已知别名（含自定义列名）的**列数**，命中 ≥2 列的行视为表头，其上行全部丢弃。

**进度归一化**：先剥掉 `%`（含全角 ％）和空格 → 数值化；`0 < 值 < 1` 视为小数比例自动 ×100（0.5 → 50%）；>100 或 <0 不钳制（留给体检提示"超100%"）。

**日期解析**：8 种正则（ISO / 斜杠 / 点号 / 中文年月日 / 美式月日年 / 年月）＋ 纯数字按 Excel 序列日（1899-12-30 起算）。美式日期单独优先处理，避免 `8/10/2026` 被误读。

**文本任务提取（text_parser）——"证据链"逻辑**：逐行判断是不是任务条目：
1. **忽略词**（规则库）命中 → 整行跳过；
2. 项目符号行：有百分比/状态词/动作词（负责/进度/完成/上线…）即视为任务；
3. 非项目符号行：必须同时具备"硬证据"——百分比 **或** 日期 **或** 负责人模式（负责人/xx负责/xx 负责），防止"下周重点：完成联调并准备上线发布"这类叙述被误抓；
4. 有任务特征但证据不足的行 → 记为**模糊行**，只在 AI 开启时连同规则命中行一起送 AI 分类（省 Token）。
任务名 = 去符号 → 在断点词（负责/进度/完成/计划…）处截断 → 尾部疑似中文人名按"是否仍含任务关键词（开发/模块/联调/重构…）"决定剥离。

### 5.2 规则引擎（engine.py）

**状态归一化优先级**（`_eval_task`）：
```
显式状态 ∈ {已完成/已滞后/有风险}（含 已上线/已交付/延期/搁置(用户映射) 等变体）→ 直接采用（人工判断优先）
否则按：实际完成日期 → 进度≥100 → 进度≤0 → 有进度(进行中) → 显式状态 → 保守默认「未开始」
```

**风险分级（五条独立规则，取最高级，等级：正常<关注<风险）**：
1. **超期**：今天 > 计划完成 → 关注；超期 ≥ 阈值(默认7天) → 风险；
2. **临近**：距计划完成 ≤ 阈值(默认3天) 且未完成 → 关注；
3. **未按时启动**：今天 > 计划开始 且状态=未开始 → 关注（晚 N 天）；
4. **显式状态**：文件标"已滞后"→ 至少关注；标"有风险"→ 风险；
5. **进度偏慢**：目标进度 − 实际进度 > 阈值(默认40%) → 关注（勾选"偏慢豁免"的任务除外）。

> 注意：风险等级比较用**等级表**（`_RISK_LEVELS`），不能用字符串 `max()`——中文按编码比较会出错（`max("正常","关注")` 恒返回"正常"），这是修过的一个真实 bug。

**目标进度**：`(今天 − 计划开始) / (计划完成 − 计划开始)`，可换 S 型曲线（逻辑斯蒂：前期慢后期快），减少前期任务被误报偏慢。

**依赖风险传递**：任务 B 填了"依赖任务 A"，若 A 滞后/有风险，B 自动升到至少「关注」并注明原因。

**关键路径（CPM）**：以 `depends_on` 建依赖图，任务权重=计划工期（缺省1天），用记忆化 DFS 求"依赖链总工期最长"的链（有环自动跳过环内边）。链上任务标 `is_critical`（表格 🔑 标注）。

**完成率与权重**：默认 `已完成数/总数`；只要任一任务权重 ≠1，改为 `已完成权重/总权重`（关键任务权重调大后完成率更真实）。

**健康体检**：缺进度/缺负责人/缺计划起止/进度>100/日期倒挂(完成早于开始)/任务名过短——前端红色提示并可一键进入校对补数据。

### 5.3 报告生成（report.py）

**模板**：blocks 是 JSON 数组，每个元素一种模块（标题/KPI/数据表/风险清单/环比…），由对应渲染函数按真实统计填充；默认 HTML 模板用 `{project_name} {period} {overview} {stats_html} …` 占位符替换。无 AI 时综述/计划用**规则回退文案**（从统计数字生成，保证离线可用）。

**AI 数字校正（防编造）**：AI 写的百分比先与真实统计核对——数值差 >1.5 且附近有关键词（完成率/平均进度…）→ 替换为真实值；否则替换为最接近的统计值；界面提示"已校正 N 处"。

**环比（与上周对比）**：读 `stats_history` 中**同名项目**最近一期，输出四类对比：①常规（完成率/风险/滞后）②**同任务集合完成率**（只比两周都存在任务，剔除新增/删除影响）③**连续两周无进展**（进度未变且未完成）④任务总数变化。

### 5.4 AI 层（ai.py）

**调用参数**：默认模型 `deepseek-chat`（可换 reasoner/自定义地址，设置页保存到 keys.json）；温度 综述 0.6 / 提炼 0.3 / 模板 0.3（提取类要确定）；max_tokens 综述 400 / 提炼 800 / 模板 1500；超时 60s；网络错误重试 1 次；401 等 HTTP 错误不重试。

**缓存**：key = SHA1(站点+模型+接口地址+完整消息)，命中则 0 Token 复用，7 天 TTL、300 条上限。

**提炼 schema 校验**：AI 返回的 JSON 逐条校验——name 必填且 ≥2 字；progress 非法/越界 → 钳制为 None；status 不在标准集合 → 置空；空名条目拒绝。防止半截 JSON 污染统计，校验失败整体回退规则结果。

### 5.5 规则库与回写闭环（rules.py + 前端）

三类自定义规则存在 `config/rules.json`：
- **ignore_keywords**（忽略词）：文本行/任务名命中即跳过；
- **column_aliases**（列名映射）：`{表头: 字段}`，参与表头识别与列映射；
- **status_words**（状态词映射）：`{变体: 标准状态}`，进引擎归一化。

**回写闭环**：校对页删除任务 → 其名记入待回写列表 → 保存时 `POST /api/rules/learn` 写入忽略词；用户把某任务的非标准状态改成标准状态 → 自动学习映射。**规则越用越准、全部本地持久化**。

---

## 六、API 清单（app.py）

| 分组 | 接口 | 说明 |
|---|---|---|
| 工作区 | GET /api/workspace | 完整工作区（sheet/分组/汇总/体检/解析度量/AI快照） |
| | POST /api/workspace/load | 上传文件（Excel 多 sheet 拆分为多个） |
| | POST /api/workspace/sheet/{sid}/delete·rename | 删除/重命名 sheet |
| | POST /api/workspace/sheet/{sid}/tasks | 保存校对后的任务（自动重算统计） |
| | POST /api/workspace/groups* | 分组增删/移入 |
| 规则 | GET/POST /api/rules | 读取/保存规则库 |
| | POST /api/rules/reset | 恢复默认（归档历史） |
| | POST /api/rules/learn | 校对回写（忽略词/状态词学习） |
| | GET /api/rules/export · POST /api/rules/import | 规则库导入导出 |
| | GET /api/rules/history | 规则历史快照 |
| 模板 | GET/POST /api/template · /reset · /parse | 模板读写/重置/AI 解析上传模板 |
| 周报 | POST /api/generate | 生成周报（含环比/关键路径/忽略计数/token 用量） |
| | POST /api/export | 导出 Word/纯文本 |
| | POST /api/export/tasks | 导出任务明细 CSV |
| 历史 | GET /api/history | 列表（含项目分组字段、环比徽标） |
| | GET /api/history/{name} · /delete · /rename | 查看/删除/重命名 |
| | POST /api/save_report | 保存历史 + 统计快照 |
| | GET /api/settings/trend | 趋势数据（画折线图） |
| 设置 | GET/POST /api/settings/key · /delete · /test | API Key 管理（脱敏/本地） |
| | GET/POST /api/settings/ai | AI 模型/接口地址配置 |
| | GET /api/settings/tokens · /clear | Token 用量统计 |

---

## 七、测试体系（tests/）

| 文件 | 覆盖 |
|---|---|
| test_golden.py + examples/golden/ | 6 个输入文件的解析基线回归（召回率/字段准确率），防意外改动 |
| test_engine_rules.py | 引擎 14 项规则单测（风险分级/依赖/豁免/权重/曲线/关键路径/等级比较） |
| test_custom_rules.py | 路线C 规则库 6 项（忽略词/列名映射/状态词/learn 去重校验） |
| test_ai_optimize.py | AI 缓存/Token 统计/schema 校验/模型配置 6 项 |

运行：`cd pm-report-agent && .venv/bin/python tests/test_golden.py`（其余同理）。

---

## 八、已知边界与待完善（见 ROADMAP）

- **定时自动周报**（周五自动汇总+推送）—— 已列入待完善事项
- 多用户/权限、Docker 化、GitHub Release
- 图表增强（燃尽/甘特）、Excel 解析结果导出（当前仅 CSV）
- 解析质量度量的"置信度评分"与 CI 接入
- OCR 精度提升（复杂版式）
