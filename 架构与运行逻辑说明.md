# PM Report Agent · 架构与运行逻辑说明

> 版本 0.33 ｜ 面向使用者/维护者的底层运行逻辑阐释
> 目标：让不了解代码的人也能明白——**每个代码文件干什么、数据是怎么一步步变成周报的**。

---

## 一、这是什么

把 Excel / CSV / Word / PDF 等**混合格式**的项目资料，通过 **AI 分析**自动提炼为结构化项目进度（任务/指标/里程碑/风险/决策），并**按你的模板原地更新**生成专业周报。**主体是 AI**：不配 API Key 时只保留原始文件，不产生任何规则推算的假数据。

启动：`python -m uvicorn app:app --port 8000` → http://127.0.0.1:8000

---

## 二、目录结构与各代码文件职责

```
pm-report-agent/
├── app.py                      ★ Web 后端（FastAPI）：全部 HTTP 接口、模板库/分组/留痕端点
├── run.py / pmo_report/cli.py  CLI 入口（旧版，仍可用）
├── web/index.html              ★ 前端单文件面板（原生 JS）：数据/看板/模板&生成 三个标签页
├── web/libraries.html          数据库中心入口（链接到 web/lib/ 各库页）
├── web/lib/                    各独立库页面 + 组件（chatbox/formkit/cal）
├── pmo_report/
│   ├── ai.py                   DeepSeek 客户端：Key 管理、调用缓存（相同请求不重复付费）
│   ├── ai_analysis.py          分析 AI：上传即分析 → 提取结构化条目；处理约定+性质标记注入
│   ├── ai_dialogue.py          通用对话弹窗后端（规则/模板微调/文档导入/看板定制 多轮对话）
│   ├── ai_pipeline.py          文档转 markdown、AI 分析辅助
│   ├── prompts.py              3 类提示词（分析/生成/规则配置）+ 版本历史/回退
│   ├── datastore.py            SQLite：源文件/条目/数据集板块/模板库/分组/留痕/提示词版本
│   ├── rules.py                规则配置（参数 → 分析 AI 处理指令；分析/生成要求）
│   ├── dataset.py              xlsx 数据集板块解析
│   └── parsers/                文本/表格解析器
├── config/                     运行期配置（全部 gitignored：keys/rules/prompts/对话配置）
├── data_sources/               上传文件归档 + catalog.sqlite（运行期生成，gitignored）
└── examples/                   示例数据
```

---

## 三、端到端数据流（一张图）

```
 上传文件（Excel/CSV/Word/PDF）
        │
        ▼
 ① 归档到 data_sources/files/ + 写 sources 表（原始文件永久保留）
        │
        ▼
 ② 分析 AI（ai_analysis.py）
    · xlsx → 数据集板块解析（表头+行）；其他 → markitdown 转文本
    · 注入【分析要求】：用户 requirements + 处理约定参数（超期/偏慢/忽略词/状态词/列名映射）
      + 性质标记约定（每条分析要求 title 即性质标签）
    · 输出结构化条目：{kind: task|risk|issue|decision|milestone|metric, name, fields}
      fields 含 marks:[性质标签] + mark_reasons 供看板/数据面板统计
    · 写入 items 表（分类仓）
        │
        ▼
 ③ 看板（/api/dashboard）：只展示 AI 提取条目，按 kind/板块分组
    · KPI 卡 + 分类区块 + ★性质专区（命中 marks 即统计）
    · ⚙ 定制看板：对话 → 配置 JSON → localStorage → 前端按配置渲染（日常零 token）
        │
        ▼
 ④ 生成（/api/generate/one）：
    · 数据源：单选 source_id 或分组 source_ids（汇总组内所有源的数据集板块 + AI 条目）
    · 模板：库中模板（优先原文 source_text）→ 原地更新语义
    · 生成 AI：在模板原文上替换与数据对应的位置，其余文字/风格/格式原样保留
        │
        ▼
 ⑤ 周报 HTML（可导出 Word / 存入文稿库）
```

---

## 四、核心机制

### 1. 主体是 AI，规则只是指令
- 规则库参数（delay_days_danger 等）不再由引擎计算，而是 `build_processing_rules()` 转成「处理约定」文本注入分析 AI；
- 分析要求条目即「性质」：`build_mark_convention()` 生成性质标记约定，AI 判断条目是否命中并输出 `marks`/`mark_reasons`；
- 无 API Key → 上传只保留原文，不产生假数据。

### 2. 提示词 3 类
- `ai_analysis`（分析）、`ai_generation`（生成）、`ai_rules`（规则配置），各一份 system+user，`{task_type}` 区分任务；
- 提示词库可编辑、每次保存记一版、可回退；旧 key 经 `LEGACY_PROMPT_MAP` 兼容映射。

### 3. 对话弹窗
- `chatbox.js` 通用弹窗：多轮往返、窗口内记忆（不持久化）、确认完成 → finalize 结构化结果；
- 每用途可配模型/温度（config/ai_dialogue.json）；规则对话/模板微调/文档导入/看板定制全部走它。

### 4. Token 友好
- AI 调用本地缓存（相同请求不重复付费）；
- 看板定制：只定制时调一次 AI（输出配置 JSON），日常渲染零 token；
- 分析只送必要文本（截断 20000 字符、max_tokens 6000）。

---

## 五、数据表（SQLite: data_sources/catalog.sqlite）

| 表 | 作用 |
|----|------|
| sources | 源文件（id/文件名/路径/上传时间/分组 group_name） |
| items | 分类仓条目（kind/name/payload(含 fields,marks)/来源） |
| dataset_sections | xlsx 数据集板块（表头+行，供生成抓数） |
| templates | 模板库（html + source_text 原文） |
| source_ops | 源数据操作留痕（删除快照可撤销） |
| prompt_versions | 提示词版本历史 |

---

## 六、API 速览（app.py）

- 源：`GET/POST /api/sources`、`GET /api/sources/{sid}`、`DELETE /api/sources/{sid}`、`/api/source-groups`、`POST /api/sources/{sid}/group`
- 看板：`GET /api/dashboard`
- 模板：`POST /api/template/parse`、`GET/POST /api/template/lib`、`POST /api/template/tune`
- 生成：`POST /api/generate/one`（source_id / source_ids + template_id）
- 对话：`POST /api/dialogue/chat|finalize`、`GET/POST /api/dialogue/config`
- 规则：`GET/POST /api/rules`、`POST /api/rules/delete|batch-delete|converse/confirm`
- 提示词：`GET /api/settings/prompts`、`POST /api/settings/prompts/save`、`/history|rollback`
- Key：`POST /api/settings/key|test`、`DELETE /api/settings/key`

---

## 七、测试体系（tests/）

`python -m pytest tests/ -q`（38 个用例全过）：
- 解析器（CSV/GBK/混合格式/docx 文本提取）
- 数据集板块解析、AI 分析 JSON 容错解析、处理约定生成
- 规则库增删改/批量删除/文档导入、提示词保存/版本回退、对话配置
- 重名上传（覆盖/改名）、源文件删除快照与撤销

---

## 八、已知边界

- 分析 AI 输出质量依赖提示词与处理约定；字段名不统一时前端做兼容（focus/level/marks）
- 看板定制配置存浏览器 localStorage（换浏览器需重新定制）
- 旧 CLI（run.py）保留但主路径已走 Web 面板
