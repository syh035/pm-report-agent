# PM Report Agent · 完善规划（Roadmap）

> 当前版本：**0.7.0**（2026-08-24）｜ 原则：**先稳定核心链路 → 再补体验 → 最后产品化**

---

## ✅ 已完成（v0.7.0 · 离线规则与筛选优化）

| 功能 | 说明 |
|------|------|
| 未按时启动预警 | 计划开始已过仍未开始的任务 → 「关注：未按计划启动（晚 X 天）」（原来完全无提示） |
| 显式状态优先 | 文件明确标注「已完成/已滞后/有风险」（含已上线/已交付/延期/高风险等变体词归一化）优先于进度自动推导，人工判断不再被丢弃 |
| 风险等级比较修复 | 原代码 `max("正常","关注")` 按中文编码比较会错误返回"正常"，导致"临近计划完成"永远无法升级到关注——已改为等级表比较 |
| 进度 0-1 归一化 | Excel 里 `0.5` 表示 50% 的常见写法自动 ×100 |
| 合并/汇总加权 | 多 sheet 完成率、平均进度按任务数加权（原为简单平均，大小表权重失衡） |
| 环比仅同名项目 | 找不到同名项目的历史记录时不再拿别的项目对比，避免误导 |
| 归一化状态筛选 | 引擎输出 `status_norm`，表格"状态"列与筛选下拉统一用标准状态（选"已完成"能筛出"已上线"） |
| 表格分页/空态/重置 | 每页 100 行 + 分页控件 + 无匹配提示 + 一键重置筛选 |
| 列名别名扩充 | 补充截止/deadline/完成进度/责任部门/开始时间等 20+ 常见叫法 |

## ✅ 已完成（v0.6.0 · AI 成本与准确性优化）

| 功能 | 说明 |
|------|------|
| AI 结果缓存层 | 综述/提炼/模板解析三处统一走磁盘缓存（`history/ai_cache.json`，7 天 TTL，300 条上限）：相同输入 0 Token 复用 |
| 规则预筛瘦身 | Word/PDF 提炼只把规则命中的候选行（去重、≤3000 字）发给 AI，不再整篇原文 → 省 80%+ 输入且更准 |
| Token 用量统计 | 设置页「AI Token 用量」卡片：累计输入/输出/调用次数/缓存命中/估算费用 + 最近记录；生成消息显示本次消耗 |
| 失败重试 | 网络类错误自动重试 1 次；HTTP 错误（401 等）不重试直接回退，不浪费 Token |
| max_tokens 收紧 | 综述 400 / 提炼 800 / 模板 1500（原 2000/1500/3000），配合载荷瘦身（任务清单截断到 10 条） |
| 黄金数据集回归 | `tests/`：3 个示例文件的解析基线 + 准确率测试（召回率/字段准确率），改解析器有数字背书 |
| 生成前体检确认 | 数据有缺项时生成前弹确认，避免基于残缺数据花钱生成 |

## ✅ 已完成（v0.5.0）

| 功能 | 说明 |
|------|------|
| 任务解析校对 | 数据面板「✏ 编辑任务」：可直接改任务名/负责人/进度/日期/状态/备注，增删任务，保存后自动重算统计 |
| 数据体检报告 | 每个 sheet/分组顶部显示缺失项（缺进度/负责人/起止日期/超100%），列出具体任务名，点击可补 |
| AI 数字校正 | AI 综述/计划生成后自动核对数字：编造的百分比按最近关键词修正为真实统计值，界面提示「已校正 N 处」 |
| 目标进度曲线 | 规则配置新增「进度曲线」：线性 / S 型（前期慢后期快），减少前期任务误报偏慢 |
| 偏慢豁免 | 任务可勾选「偏慢豁免」，该任务进度偏慢不再标红（超期/风险照常判断） |
| 风险依赖传递 | 任务可填「依赖任务」，被依赖项滞后/有风险时，本任务自动标「关注」并注明原因 |
| 周报环比 | 存历史时记录统计快照；下次生成自动输出「六、与上周对比」（完成率/平均进度/风险/滞后），模板新增「环比上周」模块 |
| 任务明细导出 | 「导出明细 CSV」：把当前 sheet/分组/全部任务导出为 CSV（utf-8-sig，Excel 打开不乱码） |
| API Key 本地设置 | 面板「设置」页填写 DeepSeek Key，仅存本机 config/keys.json（600 权限、gitignore），接口只回脱敏信息 |
| Bug 修复 | `app.py` 补 `import json`（template_blocks 通道恢复）；占位 Key 不再当真实 Key 发送 |

---

## P0 · 收尾与稳定性（1–2 天）

| # | 事项 | 说明 | 验收标准 |
|---|------|------|----------|
| 1 | 修复 `generate` 的 `template_blocks` 通道 | 已补 `import json`；补一条自动化回归：直接 POST blocks 参数能生效 | curl 传 blocks 生成成功且画布块生效 |
| 2 | 全局异常兜底 | `app.py` 加 FastAPI exception handler，解析/生成/导出失败统一返回 `{error}` 而非 500 裸栈 | 上传坏文件面板显示友好错误 |
| 3 | `_merge_stats` 完成率加权 | 多 sheet 合并时按任务数加权平均，而非简单平均 | 与手算结果一致 |
| 4 | 历史文件路径安全 | `view_history` 用 `basename` 已防穿越；补统一校验函数并覆盖 `save_report` | 路径穿越测试用例通过 |
| 5 | 前端空状态与 loading | 上传/生成/导出加 loading 态，防重复点击 | 面板操作不卡死、不重复提交 |

## P1 · 功能完善（3–5 天）

### AI 链路（配好 Key 后验证）
- [ ] **模板 AI 分块**：`/api/template/parse` 升级为返回多个 blocks（标题/KPI/表/风险分块），替换当前"单个 HTML 自定义块"
- [ ] **AI 提炼回退提示**：Word/PDF 解析时若 AI 提炼失败，在 sheet 上标注"规则解析 N 条，AI 未生效"
- [ ] **AI 输出校验**：对 AI 返回 JSON 做 schema 校验（含容错、重试一次）
- [ ] **超时与限流**：`call_deepseek` 支持重试 1 次 + 可配超时；面板展示 AI 耗时

### 数据与展示
- [x] **解析结果导出**：已实现 `/api/export/tasks`（CSV，utf-8-sig），面板「导出明细 CSV」
- [ ] **多 sheet 并行解析**：上传多个大文件时用线程池并行，前端显示进度
- [ ] **图表**：数据面板加趋势图/燃尽图（前端可引入轻量 ECharts，纯本地）
- [ ] **Excel 多 sheet 支持**：一个 .xlsx 含多张表时，每张表生成独立 sheet（当前只读第一个 sheet）

### 规则与模板
- [ ] **规则导入/导出**：rules 支持导出 JSON、导入（含从历史回滚按钮）
- [ ] **多套模板**：预置「决策简报」「管理层周报」模板，模板库可切换/复制

## P2 · 协作与产品化（1–2 周）

- [ ] **多用户/权限**：简单用户系统（本地 SQLite：用户表 + 项目归属 + 操作审计日志）
- [ ] **定时自动周报**：`schedule`/`APScheduler`，每周五自动汇总并邮件/飞书/企业微信推送
- [ ] **Docker 化**：Dockerfile + docker-compose（含 tesseract/poppler 依赖），一键 `docker compose up`
- [ ] **GitHub Release**：补齐 README 截图、演示视频脚本、CHANGELOG；打 tag 发 Release
- [ ] **配置中心化**：把 keys/rules/templates 收敛到一个 `data/` 目录（持久化卷），与代码分离
- [ ] **数据面板扩展**：甘特图、资源负荷视图

## P3 · 长期可扩展

- [ ] 接入更多模型（OpenAI 兼容接口通用化，模型名可配）
- [ ] PDF OCR 升级（PaddleOCR 或云端 OCR），复杂版式识别率提升
- [ ] 前端工程化：拆分为 Vue/React SPA + 构建产物（当前单文件 index.html 已够用，视复杂度再定）
- [ ] 单元测试覆盖（parsers / engine / report 各层），CI 接入 GitHub Actions

---

## 建议的执行顺序

1. **先做 P0**：把核心链路（上传→统计→生成→导出→历史）跑稳，所有接口有错误兜底；
2. **再配 Key 联调 AI**（P1）：当前 AI 功能从未真实联调过，务必先验证再扩展；
3. **按面试/演示诉求取舍**：若要现场演示，优先「图表 + 多套模板 + Docker」三个亮点；
4. 每一步完成后跑一遍 `examples/input/进度表_示例.csv` 全链路回归。

## 环境备注

- 本机无 conda；已用系统 Python 3.9.6 建 `.venv`（依赖见 requirements.txt）。
- PDF OCR 需要 `brew install tesseract tesseract-lang poppler`（可选，纯文本 PDF 不需要）。
- 启动：`.venv/bin/python -m uvicorn app:app --port 8000` → http://127.0.0.1:8000
