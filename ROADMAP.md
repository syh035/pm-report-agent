# PM Report Agent · 完善规划（Roadmap）

> 当前主线：**AI-first**（主体是 AI：分析提取 → 看板 → 模板原地更新生成）｜ 原则：先稳定核心链路 → 再补体验 → 最后产品化

---

## ✅ 已完成

### AI-first 重构（v0.24 → v0.33）
- **主体归 AI（v0.24）**：上传只存文件 + AI 分析入库；看板只展示 AI 提取条目；生成只用 AI 分析结果；规则参数转为「处理约定」指令注入分析 AI（AI 按约定处理，主体是 AI）
- **数据面板表格化（v0.26/0.28）**：删除遗留工作区树；源文件点击即看 AI 提取；Excel 式表格（按 kind 分板块、标题行=字段并集、长文本整段）
- **模板原地更新（v0.28）**：模板解析保留原文（source_text）；生成 = 在模板原文上替换与数据对应的位置，风格/用词/格式原样保留；支持分组（一个周报引用整组多数据集）
- **性质标记贯穿（v0.30）**：分析要求条目即性质（title=标签），AI 输出 marks/mark_reasons；数据面板每性质一列（可自定义显隐）、看板每性质 KPI + 置顶专区
- **看板定制 AI（v0.31/0.32）**：对话式调整看板布局（顺序/显隐/颜色/文案），配置存本地（上传 GitHub 他人各自定制）；只读访问配置库（性质清单/处理约定参数注入）
- **对话弹窗体系（v0.20/21）**：规则对话、模板微调、规则文档导入、看板定制 全部弹窗式多轮对话，每用途可配模型/温度
- **提示词 3 类（v0.29）**：分析 / 生成 / 规则配置（删除意图 AI 与 Agent 一句话入口）
- **常规迭代**：分组引用生成（v0.28）、重名文件改名修复（v0.27）、撤销链路改 AI 重分析（v0.25）、模板库/文稿库/源数据留痕等基础能力

## ⏸ 待完善事项（本轮不做）

- [ ] 定时自动周报（schedule + 邮件/飞书/企业微信推送）
- [ ] 自然语言检索历史（届时再评估向量库）
- [ ] 多用户/权限、Docker、GitHub Release
- [ ] Excel 解析结果导出（当前仅 HTML/Word 导出周报）
- [ ] OCR 精度提升

## 迭代原则

1. 主体始终是 AI——任何"规则推算"都改为注入 AI 的处理指令，禁止产生 AI 之外的假数据
2. 一切 AI 工作思路（分析/生成要求）归属规则库；提示词可编辑、版本可回退
3. 看板数据按分组/性质呈现，绝不混排
